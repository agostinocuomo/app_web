import 'bootstrap';
import './bootstrap';

$form1 = document.querySelector(".form1").addEventListener('submit', function(event) {
    form1.addEventListener('submit', function(event) {
        event.preventDefault(); // ✅ Blocca l'invio predefinito (es. refresh pagina)
        
      });
});

